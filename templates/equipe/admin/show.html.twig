{% extends 'backoffice/base.html.twig' %}

{% block title %}Equipe{% endblock %}

    {% block content %}

    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ equipe.nom }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ equipe.logo }}</td>
            </tr>
            <tr>
                <th>Game</th>
                <td>{{ equipe.game }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ equipe.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipe_index_admin') }}">back to list</a>

    <a href="{{ path('app_equipe_edit_admin', {'id': equipe.id}) }}">edit</a>

    {{ include('equipe/admin/_delete_form.html.twig') }}
{% endblock %}
