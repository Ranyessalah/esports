{% extends 'admin_base.html.twig' %}

{% block title %}New Training - Becem{% endblock %}

{% block admin_content %}
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-primary text-white py-3">
                <h3 class="card-title mb-0">Create New Training Session</h3>
            </div>
            <div class="card-body p-4">
                {{ include('training/_form.html.twig') }}
                
                <div class="mt-4">
                    <a href="{{ path('app_training_index') }}" class="btn btn-outline-secondary">Back to list</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
